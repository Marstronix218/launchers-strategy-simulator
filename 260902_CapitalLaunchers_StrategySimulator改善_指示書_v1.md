# Launchers Strategy Simulator 改善指示書（Noriaki氏宛）

対象サイト：https://launchers-strategy-simulator.vercel.app/

**背景：** プロトタイプv1として2026-08-28にテスト検証を実施。バグ調査の結果と、Brianからのデザイン・コンテンツ面のフィードバックを統合した、v2向けの修正依頼一覧。優先度順に記載。

**前提の確認：** 当初検討していた「20の質問」（3択・定性診断・23問）は不採用とし、このシミュレーター（財務実測値→将来予測）の方向で進めることを2026-09-02に確定。財務入力は当初「過去3期分」で設計していたが、同日中に「直近1期のみ入力し、前々期・前期は自動算出（編集可）」という簡略化に変更（詳細は優先度3.5）。同日中に「LINE登録を診断の前に移動する」案も一度検討したが、**シミュレーター体験そのものが個別相談まで進む動機付けになる**という趣旨に立ち返り、「診断→結果→LINE登録→個別相談」という元のフローのまま進めることで最終確定。以後、この方向をベースに改善する。

**優先度0（最優先）：AI分析機能のエラー修正**

**現状の問題：** 「AIによる経営示唆」セクションが常に失敗する。/api/diagnosis-insight へのPOSTリクエストが2回とも500エラー（FUNCTION_INVOCATION_FAILED）で返ってきている。バックエンド関数がサーバーサイドで例外を投げてクラッシュしている状態。

**対応してほしいこと：** まずこの機能を正常に動く状態に戻す。他の修正より先にここから着手してほしい。原因調査のため、VercelのFunction Logsでこのエンドポイントのエラースタックトレースを確認するのが早いはず。

**優先度1：「個別相談を予約する」ボタンのリンク先修正**

**現状の問題：** 結果画面末尾のCTAボタンのリンク先が https://line.me/R/（LINEの汎用URL）になっており、公式LINEアカウント「Go India!!」の友だち追加ページに接続されていない。

**対応してほしいこと：** 正しい遷移先（LINE公式アカウントの友だち追加リンク）に差し替える。**このURLはBrianから別途共有します。**

**優先度2：見出し・説明文の修正**

**2-1. 結果画面の見出し文言**

現状：「御社の10年後を確率予測しました」

変更後：「貴社の企業価値の推移をシミュレーションしてみました」

**注意：** 「AIが」という主語は付けない。この計算（モンテカルロシミュレーション）自体はAIを使っておらず、ページ内に別途「AI不使用・同じ入力なら同じ結果」という説明があるため、見出しに「AI」を入れると矛盾する。「AI」という言葉は後述のAI分析セクション（優先度4）のために取っておく。

**2-2. シミュレーション回数の解説を追加**

「10,000回の確率シミュレーション」という表記の近くに、1〜2文の説明を追加する。

例：「未来の数字は1つに決め打ちできないため、過去の変動の大きさをもとに1万通りの可能性を計算し、その中央値と上下10%の幅を示しています。」

**2-3. 結果画面トップに3年推移サマリーを追加**

現状、結果画面の冒頭でいきなり「10年後の予測」の大きな数字が出てくる。その前に、**過去3期分（売上高・営業利益・最終利益）の簡単なサマリー**を先に見せる構成に変更する。「これが御社の実績です」という土台を示してから予測を見せる順番にすることで、後続の予測に対する納得感が上がる。**優先度3.5で財務入力が「直近1期のみ」になったため、このサマリーでは直近期＝ユーザー入力値、前々期・前期＝自動算出値（編集可）という違いが分かるようにする**（詳細は優先度3.5末尾）。

**優先度3（新規）：入力フォーム「会社情報」に5つの質問を追加**

**現状：** 「01 会社情報」セクションは、会社名・ニックネーム（任意）／業種（任意）の2項目のみ。

**追加してほしいこと：** 以下の**5つの質問**を同セクションに追加する（Brianのドラフト案。文言はこのまま使用）

| # | 質問 | 形式 |
|---:|---|---|
| 1 | 5年後、10年後の自社の成長イメージは見えていますか？ | 3択（はい／いいえ／わからない） |
| 2 | 後継者が引き継ぐ事業の中身は、具体的に決まっていますか？ | 3択（はい／いいえ／わからない） |
| 3 | 今のままで、優秀な社員の給与・待遇を維持し続けられますか？ | 3択（はい／いいえ／わからない） |
| 4 | 新しい市場（海外を含む）への投資を検討したことがありますか？ | 3択（はい／いいえ／わからない） |
| 5 | 今、経営上いちばん気になっていることを教えてください | 自由記述 |

**設計上の指定：**

- **この5問はすべて任意扱いにする。** 「答えていただくとAI分析がより具体的になります」といった一言を添えるとよい

- **この5問（特にQ5の自由記述）の回答は、優先度4のAI分析セクションのプロンプトに含める。** 財務数値の推移だけでなく、この定性情報を踏まえて「フィードバック・リスク・総評」を生成することで、テンプレ的でないその会社固有の分析になる

**優先度3.5：財務情報入力を「直近1期のみ」に簡略化（2026-09-02、方針確定）**

**方針変更：** 過去3期分（5項目×3期＝15個）の入力は離脱のハードルになるため、**ユーザーが直接入力するのは直近期の5項目のみとし、これを唯一の入力方法にする**（正確な数字ルートとの選択制にはしない）。前々期・前期は、以下の仕組みで自動算出する。

**新しい入力フロー**

1.  **会社情報：** 業種・資本金・売上規模を、それぞれ5分類から選択（下記ドラフト）。優先度3の5つの質問もここに含む

2.  **直近期の財務情報：** 5項目（売上高・営業利益・最終利益・現預金残高・減価償却費）を入力

3.  **直近の業績評価：** 直近の経営状況を5段階で選択

| 評価 | 意味 |
|---:|---|
| 5 | 絶好調 |
| 4 | 順調 |
| 3 | 横ばい |
| 2 | 下降気味 |
| 1 | 思わしくない |

4.  **自動算出＋確認画面：** 3の評価を使って前々期・前期の5項目を自動計算し、**編集可能な入力欄として画面に表示する。** AIの推測とズレていると感じたら、ユーザーがその場で数字を上書きできる

5.  確定後、10,000回のシミュレーションへ進む

**4の自動算出は、AIではなく単純な計算式でよい**

優先度3.5で定義した「5段階評価→成長率の目安」の対応表を、**逆方向（時間を遡る方向）に適用するだけ**で前々期・前期を算出できる。

| 評価 | 前期比の成長率目安 |
|---|---:|
| 5：絶好調 | ＋10%超 |
| 4：順調 | ＋3〜10% |
| 3：横ばい | ±3% |
| 2：下降気味 | ▲3〜10% |
| 1：思わしくない | ▲10%超 |

例：直近期の売上高 ÷（1＋成長率）＝ 前期の売上高。これをもう一度繰り返せば前々期も出せる。

**AI APIを呼ぶ必要はない。** この方が実装が軽く、速く、毎回同じ入力なら同じ結果になり、「なぜその数字か」も説明できる。結果画面の「AI不使用・同じ入力なら同じ結果」というモンテカルロ部分の説明とも矛盾しない（「AI」という言葉は優先度4のAI分析セクションのために取っておく）。

**業種・資本金・売上規模の5分類（ドラフト。Capital Launchersの顧客層に合わせて調整してほしい）**

| 資本金 | 売上規模 | 業種 |
|---|---|---|
| 300万円未満 | 1億円未満 | 製造業 |
| 300万〜1,000万円 | 1億〜3億円 | 卸売業・小売業 |
| 1,000万〜3,000万円 | 3億〜10億円 | サービス業 |
| 3,000万〜1億円 | 10億〜30億円 | 建設業 |
| 1億円以上 | 30億円以上 | その他 |

**結果画面での表示上の注意（優先度2-3・優先度5と連動する重要事項）**

前々期・前期は自動算出値（ユーザーが確認画面で編集しなかった場合は、あくまで計算式による推定）である。優先度2-3の「3年間の実績サマリー」、優先度5の「業績推移グラフ」のどちらでも、**直近期＝ユーザー入力値、前々期・前期＝自動算出値（編集可）**という違いが分かるようにする（例：前々期・前期にはグラフ上で破線や別色、サマリー表に小さく「自動算出」の注記を入れる等）。ユーザーが確認画面で上書きした場合は、その期は「入力値」として扱ってよい。

**優先度4：AI分析セクションの拡充とラベル変更**

**ラベル変更：** 「AIによる経営示唆」→「AIによる簡易分析」

**内容拡充：** 入力された3期の推移と、優先度3で追加した5問の回答（特にQ5の自由記述）を踏まえ、財務コンサルタントの視点から以下を生成する。

- 簡単なフィードバック

- リスク

- 総評

- 5段階レーティング

**注意点：**

- 優先度0でこの機能自体のエラーを先に直すこと。動いていない機能に内容を足すと、直すべき範囲が広がるだけになる。

- 5段階レーティングを含む以上、**「本診断は概算値による簡易試算であり、会計・税務・投資判断を代替するものではありません」という断り書きを、AI分析ボックスの直下にも表示する**（現状はページ最下部のみ。AI分析部分だけスクリーンショットで切り取られて独り歩きするリスクがあるため）。

**優先度5：業績推移グラフの実績／予測の視覚的分離**

**現状の問題：** 「業績推移」グラフで、実績3期分（前々期・前期・直近期）と「10年後（中央値）」のシミュレーション結果が同じ1本の折れ線でつながっており、性質の異なるデータが滑らかにつながって見えるため、あたかも数値が収束していくかのように誤読される。

**対応してほしいこと（具体的な仕様）：**

- 実績部分（前々期→前期→直近期）は現状通り実線

- 「直近期」から「10年後（中央値）」への区間は**破線**に変更

- 「直近期」の点に「現在」というマーカー・ラベルを置き、実績と予測の境目を明示

- グラフ直下に「実線＝実績（過去3期）／点線＝シミュレーション中央値」という1行キャプションを追加

- 可能であれば、破線の周りに上位10%〜下位10%のレンジを薄い帯（面）で表示し、「点ではなく幅で見る」という考え方を視覚的にも一致させる

企業価値の推移グラフ（現在→5年後→10年後）も同様の考え方で、5年後・10年後の区間を破線にする。

**優先度3.5（簡易入力モード）を実装する場合：** 簡易ルートで前々期・前期がAI推定値になったケースでは、「実績＝実線」というこのルールをそのまま適用しない。前々期・前期は推定値であることが分かる見た目（破線・別色・「推定」ラベル等）にし、直近期（ユーザー入力の概算値）とは区別する。

**優先度6：個別相談への橋渡しコピーの追加**

現状の「なりたい10年後との差を、一緒に整理しませんか？」というCTA手前に、以下の文章を追加する。

> 今回の診断は、「このまま何もしなければ」という前提での未来です。  
> 実際には、経営判断ひとつで数字は変わります。
>
> たとえば、こんな選択肢が考えられます。
>
> - 成長市場への事業投資
> - M&Aによる事業成長
> - 海外人材の活用
> - 中期事業計画の策定
> - 海外企業との業務提携
> - AI活用による業務改善
>
> どれが御社に合うかは、数字だけでは判断できません。  
> 無料の個別相談で、診断結果の背景とあわせて一緒に整理します。  
> Goだけでなく、No-Goも大切な経営判断です。

**狙い：** いきなり「インド進出」を前面に出すとスルーされやすいため、まず普遍的な関心事（企業の将来性）から入り、解決策の選択肢を並べたうえで個別相談へ誘導する。全ユーザー共通の内容でよい。デザインは、ページ上部で使っている「無料・登録不要・約2分」のようなバッジ/チップの見た目で6項目を並べると、既存デザインとトーンが揃う。ここまでシミュレーター体験を通じて関心を積み上げたうえで、末尾のLINE登録（＝個別相談への入口）に自然につなげるのが狙い。

**優先度7：AIへの財務データ送信先の明記**

優先度4でAI分析の内容を拡充するため、現状の「財務数値は診断計算とAI分析のために送信しますが、本アプリのデータベースには保存しません」という文言をもう一歩具体化する。使用しているAIサービス名・データが保持されないことを明記し、経営者が実際の売上・利益・現預金といった機密情報を入力する際の心理的抵抗を下げる。優先度3で5問（特に自由記述）も送信対象に加わるため、この文言でカバーする範囲を財務数値だけでなく回答内容全般に広げる。

**優先度8：配色・レイアウトの見直し**

**配色：** 現状のCapital Launchersコーポレートカラー（赤）から、**紺地に白字**の配色に変更する。財務シミュレーションツールとして赤は「赤字」を連想させ、良い数字を見せる場面でも印象を悪くする可能性があるため。

**レイアウト：** 文字サイズ・配置・バランスを全体的に見直す。特にモバイル表示（このツールは携帯での閲覧が前提）を中心に確認する。

**参考イメージ：** 別途、紺＋白の配色イメージをBrianから共有する（結果画面・入力フォーム画面の2点）。

**優先度9：入力単位のトグル（万円／億円）**

財務数値の入力欄（優先度3.5の直近期入力・自動算出の確認画面を含む）に、単位を「万円」「億円」で切り替えられるボタンを追加する。資本金や売上高が大きい会社ほど、万円単位での入力・桁の視認が煩雑になるため。結果画面の表示単位も、入力時に選んだ単位に揃えるか、統一表示にするかはNoriaki氏の実装しやすい方でよい。

**修正後の再テスト**

優先度0〜9がすべて反映されたら、Brian側で以下を中心に再テストする。

- AI分析セクションが実際に表示されるか（5問の回答が反映された内容になっているか）

- 「個別相談を予約する」ボタンが正しいLINEアカウントに遷移するか

- 直近期のみ入力→前々期・前期が自動算出される→確認画面で編集できる、という一連の流れが動くか

- グラフ・サマリーで「入力値」と「自動算出値」が視覚的に区別されているか

- 5問セクションが任意のまま、直近期の財務数値のみでも診断が完了するか

修正が完了したタイミングで一声かけてほしい。

**関連ノート**

- 260823_CapitalLaunchers_全チャネル統合マーケティング戦略_提案_v1

- 260804_CapitalLaunchers_20の質問_v5（不採用・参考として残す）

- 260803_高木氏MTG要点レポート_v1（5問の自由記述アイデアの初出。最終的な文言はBrianのドラフト案を採用）
